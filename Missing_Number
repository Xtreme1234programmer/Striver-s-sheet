class Solution {
    public int missingNumber(int[] arr) {
        int n=arr.length;
        int currSum=0;
        int sum=(n*(n+1)/2);

        for(int i=0;i<n;i++){
            currSum+=arr[i];
        }
        return sum-currSum;
    }
}
